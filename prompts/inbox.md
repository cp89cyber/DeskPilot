Use Gmail tools to inspect the inbox and produce a structured triage report.

Requirements:
- Focus on urgent, time-sensitive, or high-value threads first.
- If a reply is clearly useful, create a staged Gmail draft with `stage_gmail_draft`.
- Extract actionable follow-ups from the threads you inspect.
- Cite all referenced Gmail threads in `sourceRefs` using the `thread:<id>` format.
- Do not claim a draft was sent. It is only staged.
