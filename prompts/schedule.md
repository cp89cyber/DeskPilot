Plan a meeting or scheduling action.

Requirements:
- Use calendar tools to inspect availability before proposing slots.
- If the request is underspecified, ask exactly one clarifying question and stop.
- When enough information exists, propose 2-3 concrete time slots.
- If a draft event is appropriate, create it with `stage_calendar_event` and report the staged action ID.
- Cite all event references in `sourceRefs`.
